# h1 plaintext :8080 -> backend:9000. GOMAXPROCS (= PROXY_CPUS) from compose
# env. Admin API off to keep it off the measured path.
{
	admin off
	auto_https off
}

:8080 {
	reverse_proxy backend:9000
}
