# The origin: must never be the bottleneck. Canned bodies from tmpfs, no
# hot-path logging, no mid-run connection churn.
#
# worker_processes from BACKEND_CPUS: `auto` counts host cores, not the quota.
worker_processes ${BACKEND_CPUS};
worker_cpu_affinity auto;
worker_rlimit_nofile 262144;
error_log /var/log/nginx/error.log warn;

events {
    worker_connections 65536;
    use epoll;       # auto-detected on Linux, explicit for documentation
    multi_accept on; # drain the accept queue in one wakeup under bursts
}

http {
    access_log off;
    log_not_found off;
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 300s;
    keepalive_requests 100000000; # no mid-run connection churn from the origin
    reset_timedout_connection on;

    # Cache the body fds: no open()/stat() on the hot path.
    open_file_cache max=64 inactive=300s;
    open_file_cache_valid 300s;
    open_file_cache_min_uses 1;
    open_file_cache_errors on;

    # gzip off (default): bodies must be byte-identical for every proxy.

    server {
        listen 9000 backlog=65535 reuseport;
        root /srv/bodies; # /64 /1k /10k /100k generated at container start

        location = /healthz {
            return 200 "ok\n";
        }
    }
}
