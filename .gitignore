.env
.env.cloud
results/
cloud/.terraform/
# cloud/.terraform.lock.hcl is committed (see cloud/versions.tf)
cloud/terraform.tfstate*
cloud/terraform.tfvars
cloud/sa-key.json
.DS_Store

# Per-run self-signed TLS pair from `bench suite` (suite.zig ensureTlsMaterial).
proxies/tls/

# devenv
.devenv*
devenv.local.nix

# direnv
.direnv

# zig build artifacts (build.zig.zon pins deps by hash)
bench/.zig-cache/
bench/zig-out/
# Vestigial: no build uses it any more; safe to delete.
bench/zig-pkg/

# rust build artifacts (proxies/pingora); lockfile not committed
proxies/pingora/target/
proxies/pingora/Cargo.lock
